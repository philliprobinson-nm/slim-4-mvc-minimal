{% include '_header.html.twig' %}

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12">
                <h1>{{ title }}</h1> 
                <p class="lead">{{ message }}</p>
            </div>
        </div>
    </div>

{% include '_footer.html.twig' %} 